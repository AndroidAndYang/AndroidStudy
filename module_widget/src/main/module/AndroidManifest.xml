<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.seabig.moduledemo.widget">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.activity.MaterDesignActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.ScreenSlideActivity" />

        <activity android:name=".ui.activity.EchelonActivity" />

        <activity android:name=".ui.activity.LifecyclePresenterActivity" />

        <activity android:name=".ui.activity.SeatTableActivity" />

        <activity android:name=".ui.activity.PieActivity"/>

    </application>

</manifest>
